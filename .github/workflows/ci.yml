name: CI

on:
  push:
    branches:
      - main
      - develop
      - misa-work
      - 'feature/*'
      - 'fix/*'
      - 'release/*'
  pull_request:
    branches: [main]
  workflow_dispatch:

# Restrict the workflow token to repository contents reads. CI does not need
# write access to issues, pull requests, deployments, or repository settings.
permissions:
  contents: read

# Cancel in-progress runs on same branch
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # CI only reads the checkout; do not leave the workflow token in git config.
          persist-credentials: false

      - name: Configure git
        run: |
          git config user.email "anurag008w@users.noreply.github.com"
          git config user.name "anurag008w"

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm run test -- --run

      - name: Type check
        run: npm run typecheck || npx tsc --noEmit

  web-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test
    steps:
      - name: Checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        env:
          VITE_DEFAULT_AI_BASE_URL: ${{ secrets.VITE_DEFAULT_AI_BASE_URL }}
          VITE_DEFAULT_AI_API_KEY: ${{ secrets.VITE_DEFAULT_AI_API_KEY }}
          VITE_DEFAULT_AI_MODEL: ${{ secrets.VITE_DEFAULT_AI_MODEL }}
          VITE_DEFAULT_AI_MODELS: ${{ secrets.VITE_DEFAULT_AI_MODELS }}
        run: npm run build

  android-build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: web-build
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Setup Android SDK
        uses: android-actions/setup-android@be39fa834029ff78f1a44aa3bb0819b8fc2bd8fd # v4
        with:
          # Install only SDK components required by this project. The action
          # accepts the standard Android SDK licenses non-interactively, while
          # avoiding unrelated add-ons (such as Google TV) that can introduce
          # unnecessary license prompts on developer machines.
          packages: 'platform-tools platforms;android-36 build-tools;36.0.0'
          accept-android-sdk-licenses: true
          log-accepted-android-sdk-licenses: false
      - name: Install dependencies
        run: npm ci
      - name: Build web app
        # cap sync copies dist/ into the Android project — build it first.
        run: npm run build
      - name: Sync Capacitor Android project
        run: npx cap sync android

      - name: Android unit tests and debug APK
        working-directory: android
        run: ./gradlew test assembleDebug --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: levelup-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 5
